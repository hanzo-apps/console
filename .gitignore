node_modules
.next/
out/
dist/

# npm is the one package manager here — package-lock.json is the tracked lockfile
# (the Dockerfile runs `npm install`). A pnpm/yarn lockfile would silently drift.
pnpm-lock.yaml
yarn.lock

# build artifacts
*.tsbuildinfo
next-env.d.ts

# env
.env*.local

# editor / os
.DS_Store
.vscode/
.idea/
*.log
e2e/screenshots/
e2e-shots/
test-results/
playwright-report/
.claude/

# blank-audit generated report
e2e/blank-report.json
