name: Build Docker Image
# NEUTRALIZED — the canonical build+deploy pipeline is now native:
# .hanzo/workflows/deploy.yml (Hanzo Git push → in-cluster act_runner →
# BuildKit builds Dockerfile → ghcr.io/hanzoai/console:<sha> → kubectl patch
# app console → operator reconcile → hanzocd). GitHub is a mirror; this
# workflow no longer builds or pushes any image.
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Native pipeline notice
        run: echo "native pipeline is .hanzo/workflows/deploy.yml; GitHub is a mirror"
